Test Environment MVP

This folder contains minimal renderer helpers for the Test Environment feature.

The real UI components should live under the main React app; these are examples for main process IPC usage.